apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: mint
  description: Sample service with CI/CD
  annotations:
    github.com/project-slug: devops-champ/mint
    backstage.io/techdocs-ref: dir:.
    sonarqube.org/project-key: devops-champ_mint
    roadie-backstage-pull-requests/default-filter: "is:open is:pr"
    platform.io/argocd-dev: mint-dev
    platform.io/argocd-qa: mint-qa
    platform.io/argocd-prod: mint-prod
spec:
  type: service
  lifecycle: experimental
  owner: group:default/platform-team
  dependsOn:
    - resource:default/orders-db
    - resource:default/kafka-cluster
